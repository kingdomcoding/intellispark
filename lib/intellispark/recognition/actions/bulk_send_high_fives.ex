defmodule Intellispark.Recognition.Actions.BulkSendHighFives do
  @moduledoc false

  use Ash.Resource.Actions.Implementation

  @impl true
  def run(_input, _opts, _context) do
    # Filled in at Phase G.
    {:ok, []}
  end
end
